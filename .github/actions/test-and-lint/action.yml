name: "Test"

description: "Runs linters, type check and test."

defaults:
  run:
    shell: bash

runs:
  using: "composite"
  steps:
    - name: Lint
      run: |
        pnpm lint
        pnpm -C=playground lint
    - name: Type check
      run: |
        pnpm -C=playground check
        pnpm check
    - name: Test
      run: pnpm test --coverage --coverage.reporter=text
